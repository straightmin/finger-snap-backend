// src/routes/collections.routes.ts
import { Router } from 'express';
import { authenticateToken } from '../middlewares/auth.middleware';

const router = Router();

// 여기에 컬렉션 관련 라우트를 추가할 예정입니다.

export default router;
